# ktyouei_sansuu01

このリポジトリには、GitHub Pages の "mhime-studylog01" をローカルで再現できるように静的サイトをコピーしています。

- `index.html` — アプリのマークアップ
- `styles.css` — レイアウトとデザイン
- `app.js` — ローカル保存・集計・グラフ表示

ローカルで確認するには:

```bash
python -m http.server 8000
```

その後、ブラウザで `http://localhost:8000` を開いてください。
